module.exports = [
  {
    anchor: 'data-lakehouse', name: 'CXOS Data Lakehouse',
    items: [
      {
        slug: 'lakehouse-zones', name: 'Raw &rarr; Curated &rarr; Analytics Data Marts',
        tagline: 'The three-tier structure every event passes through inside the lakehouse — raw as received, curated after cleaning and modeling, and marts for fast analytical consumption.',
        hldCaption: 'Every zone lives on the same storage, differing only in transformation stage.',
        hld: [
          { label: 'Data Source', name: 'Data Sources', detail: 'Every touchpoint and business system' },
          { label: 'Ingestion', name: 'Ingestion Layer', detail: 'SDKs, connectors, protocols' },
          { label: 'Processing', name: 'Transformation &amp; Processing', detail: 'Cleaned, enriched, modeled events' },
          { label: 'Foundation', name: 'Lakehouse Zones', detail: 'Raw &rarr; Curated &rarr; Marts on Azure Data Lake Storage Gen2', origin: true },
          { label: 'Intelligence', name: 'Query &amp; Analytics Engine', detail: 'Reads primarily from the marts zone' },
          { label: 'Activation', name: 'Every Destination', detail: 'Ultimately sourced from lakehouse data' },
        ],
        business: [
          "Separating raw/curated/marts lets the platform keep an unmodified historical record while still serving fast, clean data to BI tools",
          "Protects against a common failure mode: 'fixing' data in place and losing the ability to reprocess from the original source",
          'Owned by Data Engineering',
        ],
        technical: 'All three zones live on Azure Data Lake Storage Gen2 as Iceberg tables, differing only in transformation stage, not storage technology. The raw zone is an append-only, schema-on-write copy of validated events (written directly by the Ingestion API). The curated zone holds deduplicated, identity-stitched, quality-checked data produced by Transformation &amp; Processing. The marts zone holds the pre-aggregated rollups and dimensional models described under Batch Processing, optimized for the Query &amp; Analytics Engine\'s access patterns.',
        chipsLabel: 'Zones', chips: ['Raw (append-only)', 'Curated (modeled, deduped)', 'Marts (pre-aggregated)'],
        artifactTitle: 'Zone Table Naming',
        artifactCode: `raw.events_2026_08
curated.identity_stitched_events
marts.revenue_by_category_daily`,
        integration: [
          'Azure Data Lake Storage Gen2 — physical storage for all three zones',
          'Ingestion API — writes directly to the raw zone',
          'Transformation &amp; Processing (Stream/Batch Workers) — populate curated and marts zones',
          'Query &amp; Analytics Engine — primarily reads from marts, falls back to curated for ad hoc queries',
        ],
        servicesConsumed: [
          'Owning microservice &mdash; <code>Cxos.Foundation.Infrastructure</code> (see the <a href="../../index.html#service-map">Full Application Service Map</a>)',
          'Database &mdash; Azure Data Lake Storage Gen2 (Iceberg) &mdash; the lakehouse itself',
        ],
        nfr: [
          'Scale: each zone scales independently; raw grows unboundedly while marts stays compact via aggregation',
          "Latency: raw zone is near-real-time; curated lags by the Stream Worker's processing time; marts lags by the batch schedule",
          'Reliability: because raw is immutable and complete, curated/marts can always be rebuilt from it if a bug is found',
          'Security/Privacy: access policies differ by zone — raw often has the tightest access since it is least processed/redacted',
        ],
        example: "A data science team needs a metric that doesn't exist in any mart yet. Because the curated zone retains full event-level detail, they can compute it directly rather than waiting for a new ETL pipeline to be built and backfilled.",
      },
      {
        slug: 'open-table-format', name: 'Open Table Format (Iceberg)',
        tagline: "The open, vendor-neutral table format underlying every zone of the lakehouse — what makes 'zero-copy' and 'query engine of your choice' actually possible.",
        hldCaption: 'Iceberg is the metadata layer that gives table semantics to files in blob storage.',
        hld: [
          { label: 'Data Source', name: 'Data Sources', detail: 'Every touchpoint and business system' },
          { label: 'Ingestion', name: 'Ingestion Layer', detail: 'SDKs, connectors, protocols' },
          { label: 'Processing', name: 'Transformation &amp; Processing', detail: 'Writes tables in Iceberg format' },
          { label: 'Foundation', name: 'Open Table Format (Iceberg)', detail: 'Table metadata layer over Azure Data Lake Storage Gen2 Parquet files', origin: true },
          { label: 'Intelligence', name: 'Query &amp; Analytics Engine', detail: 'DataFusion reads Iceberg tables directly' },
          { label: 'Activation', name: 'Any Iceberg-compatible Engine', detail: 'Spark, Trino, Snowflake can all attach' },
        ],
        business: [
          'Avoids vendor lock-in to a single proprietary warehouse format — the same physical data can be queried by multiple engines',
          "Enables the platform's zero-copy architecture: no separate copy needed per consuming tool",
          'Owned by Data Engineering / Platform Architecture',
        ],
        technical: 'Apache Iceberg provides a metadata layer (manifest files, snapshots) on top of Parquet files in Azure Data Lake Storage Gen2, giving SQL-table semantics (schema, partitions, ACID transactions) to files in blob storage. The Query &amp; Analytics Engine\'s DataFusion runtime reads Iceberg tables natively; the format\'s open specification also lets Spark, Trino, or Snowflake attach to the same tables without a data copy or export step.',
        chipsLabel: 'Iceberg Capabilities', chips: ['ACID transactions', 'Hidden partitioning', 'Snapshot isolation', 'Engine-agnostic metadata', 'Snowflake external tables'],
        artifactTitle: 'Table Metadata Reference',
        artifactCode: `{
  "table": "curated.identity_stitched_events",
  "format-version": 2,
  "current-snapshot-id": 5821093741,
  "location": "abfss://lakehouse@cxosdata.dfs.core.windows.net/curated/events"
}`,
        integration: [
          'Azure Data Lake Storage Gen2 — underlying Parquet file storage',
          'Apache Iceberg — the table format/metadata specification',
          'Query &amp; Analytics Engine (DataFusion) — primary read engine',
          'Any Iceberg-compatible engine (Spark, Trino, Snowflake via external tables) — can attach without migration',
        ],
        servicesConsumed: [
          'Owning microservice &mdash; <code>Cxos.Foundation.Infrastructure</code> (see the <a href="../../index.html#service-map">Full Application Service Map</a>)',
          'Database &mdash; Azure Data Lake Storage Gen2 (Iceberg) &mdash; the lakehouse itself',
        ],
        nfr: [
          "Scale: Iceberg's manifest-based metadata scales to very large tables without the small-file listing problems of raw Parquet/Hive tables",
          'Latency: metadata operations (snapshot listing, partition pruning) are fast regardless of table size',
          'Reliability: ACID transactions prevent readers from seeing partially written data during concurrent writes',
          'Security/Privacy: table-level access control is enforced by the Governance &amp; Security layer on top of the format',
        ],
        example: "A data science team wants to run a one-off Spark job against curated event data. Because it's stored as Iceberg, they attach Spark directly to the existing tables — no export, no separate copy, no waiting on a data engineering ticket.",
      },
      {
        slug: 'zero-copy-architecture', name: 'Zero-Copy Architecture',
        tagline: 'The principle that data is stored once and queried by many engines/consumers, instead of being copied into each tool that needs it.',
        hldCaption: 'One physical copy, many query engines — no per-tool duplication.',
        hld: [
          { label: 'Data Source', name: 'Data Sources', detail: 'Every touchpoint and business system' },
          { label: 'Ingestion', name: 'Ingestion Layer', detail: 'SDKs, connectors, protocols' },
          { label: 'Processing', name: 'Transformation &amp; Processing', detail: 'Writes once to the lakehouse' },
          { label: 'Foundation', name: 'Zero-Copy Architecture', detail: 'One physical copy, many query engines', origin: true },
          { label: 'Intelligence', name: 'Query &amp; Analytics Engine', detail: 'Reads the same physical files' },
          { label: 'Activation', name: 'Every Consumer', detail: 'AI &amp; Insights, BI tools, ad hoc analysis — no duplication' },
        ],
        business: [
          'Eliminates the storage cost and staleness risk of maintaining separate copies of data for each tool (a warehouse copy, a BI-tool extract, an ML-training copy)',
          'Every consumer sees the same data at the same freshness — no "which copy is right" debates',
          'Owned by Platform Architecture',
        ],
        technical: "Because every zone is stored as Iceberg tables on Azure Data Lake Storage Gen2, any engine that speaks the Iceberg format reads the same physical Parquet files directly — there's no ETL step that copies data into a separate proprietary warehouse format. The Query &amp; Analytics Engine, ad hoc Spark jobs, Snowflake (via Iceberg external tables), and Azure Machine Learning training pipelines all read from the same underlying storage, differing only in compute engine, not data location.",
        chipsLabel: 'Benefits', chips: ['No duplicate storage cost', 'No cross-copy staleness', 'Single access-control surface'],
        artifactTitle: 'Multi-Engine Access',
        artifactCode: `-- Query & Analytics Engine (DataFusion)
SELECT * FROM curated.identity_stitched_events;

-- Ad hoc Spark job, same physical table
spark.read.format("iceberg").load("curated.identity_stitched_events")`,
        integration: [
          'Azure Data Lake Storage Gen2 — the single physical storage layer',
          'Apache Iceberg — makes multi-engine access possible without copies',
          'Query &amp; Analytics Engine, Azure Machine Learning, ad hoc tools — all read the same tables',
          'Governance &amp; Security — single point of access control since there is only one copy to secure',
        ],
        servicesConsumed: [
          'Owning microservice &mdash; <code>Cxos.Foundation.Infrastructure</code> (see the <a href="../../index.html#service-map">Full Application Service Map</a>)',
          'Database &mdash; Azure Data Lake Storage Gen2 (Iceberg) &mdash; the lakehouse itself',
        ],
        nfr: [
          'Scale: avoids the storage cost multiplication of N tools each holding their own copy',
          'Latency: no ETL delay between "data is ready" and "every tool can see it" — it is the same moment',
          'Reliability: eliminates an entire class of bugs caused by copies drifting out of sync',
          'Security/Privacy: a single access-control surface is easier to audit correctly than N separate copies with N separate permission systems',
        ],
        example: "Previously, the BI team maintained their own extract, and the ML team maintained a separate training dataset — both drifting from the source over time. With zero-copy architecture, both read the same Iceberg tables directly, and a data quality fix in the curated zone is instantly visible to both teams simultaneously.",
      },
      {
        slug: 'time-travel-versioning', name: 'Time Travel &amp; Versioning',
        tagline: 'The ability to query the lakehouse as it existed at any point in the past — essential for auditing, debugging, and safe backfills.',
        hldCaption: 'Every write creates a new snapshot instead of mutating history.',
        hld: [
          { label: 'Data Source', name: 'Data Sources', detail: 'Every touchpoint and business system' },
          { label: 'Ingestion', name: 'Ingestion Layer', detail: 'SDKs, connectors, protocols' },
          { label: 'Processing', name: 'Transformation &amp; Processing', detail: 'Backfills rely on this capability' },
          { label: 'Foundation', name: 'Time Travel &amp; Versioning', detail: 'Iceberg snapshot history per table', origin: true },
          { label: 'Intelligence', name: 'Compliance &amp; Audit', detail: 'Reconstructs historical state for investigations' },
          { label: 'Activation', name: 'Safe Backfill Cutover', detail: 'Validate a new version before it becomes current' },
        ],
        business: [
          "Lets the business answer 'what did this customer's profile look like on this date' — critical for dispute resolution and compliance audits",
          'Makes backfills and schema changes safe by allowing validation against a snapshot before committing',
          'Owned by Data Engineering, used heavily by Compliance and Data Science',
        ],
        technical: "Every write to an Iceberg table creates a new immutable snapshot rather than mutating existing data in place. Queries can target a specific snapshot ID or timestamp (<code>SELECT * FROM table FOR TIMESTAMP AS OF ...</code>), letting Backfills validate a new snapshot before switching the 'current' pointer, and letting Compliance reconstruct exactly what data looked like at any past moment.",
        chipsLabel: 'Capabilities', chips: ['Snapshot-per-write history', 'Query as of timestamp', 'Rollback to a prior snapshot'],
        artifactTitle: 'Time Travel Query',
        artifactCode: `SELECT * FROM curated.customer_profile
FOR TIMESTAMP AS OF '2026-07-01T00:00:00Z'
WHERE customer_key = 'cust_004821';`,
        integration: [
          'Apache Iceberg — snapshot mechanism underlying this capability',
          'Backfills — use snapshot isolation to validate before cutover',
          'Query &amp; Analytics Engine — exposes the AS OF query syntax',
          'Audit Logs (Governance &amp; Security) — often cross-referenced with a specific snapshot for investigations',
        ],
        servicesConsumed: [
          'Owning microservice &mdash; <code>Cxos.Foundation.Infrastructure</code> (see the <a href="../../index.html#service-map">Full Application Service Map</a>)',
          'Database &mdash; Azure Data Lake Storage Gen2 (Iceberg) &mdash; the lakehouse itself',
        ],
        nfr: [
          'Scale: snapshot metadata overhead is small relative to data size; old snapshots are expired on a retention policy to bound storage growth',
          'Latency: querying a historical snapshot has the same performance characteristics as querying current data',
          'Reliability: rollback to a prior snapshot is a metadata operation, not a data-copy operation — fast and safe',
          "Security/Privacy: historical snapshots are subject to the same access control and retention policy as current data — time travel doesn't bypass governance",
        ],
        example: "A customer disputes a charge, claiming their subscription tier was different at the time. Compliance queries the customer profile table as of the disputed date and gets a definitive, auditable answer instead of relying on a change log that might be incomplete.",
      },
      {
        slug: 'schema-evolution', name: 'Schema Evolution',
        tagline: 'The ability to add, rename, or reorder columns in a lakehouse table without rewriting existing data or breaking existing queries.',
        hldCaption: 'Schema Evolution lets tables change shape as the business does.',
        hld: [
          { label: 'Data Source', name: 'Data Sources', detail: 'Every touchpoint and business system' },
          { label: 'Ingestion', name: 'Ingestion Layer', detail: 'SDKs, connectors, protocols' },
          { label: 'Processing', name: 'Event Schema &amp; Registry', detail: 'Governs what changes are allowed' },
          { label: 'Foundation', name: 'Schema Evolution', detail: "Iceberg's in-place schema change support", origin: true },
          { label: 'Intelligence', name: 'Query &amp; Analytics Engine', detail: 'Old and new queries both keep working' },
          { label: 'Activation', name: 'Uninterrupted Downstream Consumers', detail: 'No forced migration window' },
        ],
        business: [
          "The business changes constantly, and the lakehouse's table structure has to change with it without a disruptive migration",
          'Removes a major source of "can we add this field" friction between business teams and data engineering',
          'Owned by Data Engineering',
        ],
        technical: 'Iceberg tracks column changes (add, drop, rename, reorder, widen type) as metadata operations rather than rewriting existing Parquet files. Old files remain readable under the new schema (missing new columns read as null), and this capability is what the Event Schema &amp; Registry\'s compatibility rules are built to safely take advantage of at the lakehouse layer, not just the event-contract layer.',
        chipsLabel: 'Supported Changes', chips: ['Add column', 'Rename column', 'Reorder columns', 'Widen type (int &rarr; long)'],
        artifactTitle: 'Schema Evolution Operation',
        artifactCode: `ALTER TABLE curated.orders
ADD COLUMN tax_amount DECIMAL(10,2);
-- Existing files remain valid; new column reads as NULL for old rows`,
        integration: [
          'Apache Iceberg — provides the underlying schema evolution mechanism',
          'Event Schema &amp; Registry — governs which changes are safe to propagate into the lakehouse',
          'Data Modeling — dimensional tables evolve using this capability as business entities change',
          'Query &amp; Analytics Engine — must handle nulls gracefully for newly added columns on historical rows',
        ],
        servicesConsumed: [
          'Owning microservice &mdash; <code>Cxos.Foundation.Infrastructure</code> (see the <a href="../../index.html#service-map">Full Application Service Map</a>)',
          'Database &mdash; Azure Data Lake Storage Gen2 (Iceberg) &mdash; the lakehouse itself',
        ],
        nfr: [
          'Scale: schema changes are metadata-only operations, independent of table size — no multi-hour rewrite of historical data',
          'Latency: a schema change takes effect immediately for new writes; historical data is reinterpreted, not rewritten',
          "Reliability: schema evolution is additive-safe by design — existing queries don't break when a column is added",
          'Security/Privacy: newly added columns must still go through the same PII classification process as any new field',
        ],
        example: 'The Commerce team needs to add tax_amount to the orders table. Schema evolution lets this happen instantly without rewriting years of historical order data or breaking any dashboard currently querying the table.',
      },
      {
        slug: 'partitioning-clustering', name: 'Partitioning &amp; Clustering',
        tagline: 'The physical data-layout strategy that makes queries against a multi-billion-row table fast instead of a full scan.',
        hldCaption: 'Partitioning is the difference between a fast dashboard and a timeout.',
        hld: [
          { label: 'Data Source', name: 'Data Sources', detail: 'Every touchpoint and business system' },
          { label: 'Ingestion', name: 'Ingestion Layer', detail: 'SDKs, connectors, protocols' },
          { label: 'Processing', name: 'Transformation &amp; Processing', detail: 'Writes data using the partition strategy' },
          { label: 'Foundation', name: 'Partitioning &amp; Clustering', detail: 'Iceberg hidden partitioning by date/customer', origin: true },
          { label: 'Intelligence', name: 'Query &amp; Analytics Engine', detail: 'Partition pruning speeds up filtered queries' },
          { label: 'Activation', name: 'Interactive Dashboard Latency', detail: 'The direct beneficiary of good partitioning' },
        ],
        business: [
          'The difference between a dashboard that loads in a second and one that times out often comes down entirely to partitioning strategy',
          'Keeps query costs (compute time) manageable as the lakehouse grows into the billions of rows',
          'Owned by Data Engineering',
        ],
        technical: "Tables are partitioned primarily by event date (supporting the common 'last N days' query pattern) and, for high-cardinality access patterns, clustered by customer key. Iceberg's hidden partitioning means queries don't need to explicitly reference partition columns — the engine automatically prunes irrelevant partitions based on filter predicates in the query, unlike older Hive-style partitioning that required exact partition-column matches.",
        chipsLabel: 'Strategy', chips: ['Partition by event date', 'Cluster by customer_key', 'Automatic partition pruning'],
        artifactTitle: 'Partition Specification',
        artifactCode: `{
  "table": "curated.identity_stitched_events",
  "partition-spec": [{ "field": "event_date", "transform": "day" }],
  "sort-order": [{ "field": "customer_key" }]
}`,
        integration: [
          'Apache Iceberg — hidden partitioning implementation',
          'Query &amp; Analytics Engine — partition pruning happens transparently during query planning',
          'Rollups &amp; Aggregations — batch jobs are designed around the date-partition boundary',
          'Azure Data Lake Storage Gen2 — physical file layout follows the partition structure',
        ],
        servicesConsumed: [
          'Owning microservice &mdash; <code>Cxos.Foundation.Infrastructure</code> (see the <a href="../../index.html#service-map">Full Application Service Map</a>)',
          'Database &mdash; Azure Data Lake Storage Gen2 (Iceberg) &mdash; the lakehouse itself',
        ],
        nfr: [
          'Scale: good partitioning is what allows query performance to stay roughly constant as total table size grows',
          'Latency: a well-pruned query touches only the relevant partitions, not the entire table history',
          'Reliability: partition strategy is set at table-design time and reviewed like any other schema decision',
          'Security/Privacy: overly fine-grained partitioning can create many small files, an operational cost addressed by Lifecycle Management',
        ],
        example: "A dashboard querying 'last 7 days of revenue' against a 3-billion-row events table returns in under a second, because partition pruning means the query engine only ever touches 7 days' worth of files instead of the full table history.",
      },
      {
        slug: 'lifecycle-management', name: 'Lifecycle Management',
        tagline: 'Automated policies that age data through storage tiers and eventually expire it — balancing cost, performance, and retention requirements.',
        hldCaption: 'Lifecycle Management is what makes retention policy actually enforced, not just documented.',
        hld: [
          { label: 'Data Source', name: 'Data Sources', detail: 'Every touchpoint and business system' },
          { label: 'Ingestion', name: 'Ingestion Layer', detail: 'SDKs, connectors, protocols' },
          { label: 'Processing', name: 'Governance &amp; Security', detail: 'Retention policy is defined here' },
          { label: 'Foundation', name: 'Lifecycle Management', detail: 'Azure Blob Storage tiering + Iceberg snapshot expiration', origin: true },
          { label: 'Intelligence', name: 'Cost Management', detail: 'Directly reduces storage spend over time' },
          { label: 'Activation', name: 'Compliant Data Deletion', detail: "Enforces 'right to be forgotten' and retention limits" },
        ],
        business: [
          'Keeps lakehouse storage costs proportional to actual value, not an ever-growing bill for data nobody queries anymore',
          "The mechanism that makes retention-limit and 'right to be forgotten' compliance requirements actually enforceable",
          'Owned by Data Engineering, policy set by Data Governance/Legal',
        ],
        technical: "A scheduled .NET Core job applies lifecycle policies per table: moving older partitions from Azure Blob Storage's Hot tier to Cool/Archive tiers based on access-frequency assumptions, expiring old Iceberg snapshots beyond the time-travel retention window, and permanently deleting data that has exceeded its retention limit or is subject to an explicit deletion request (e.g., a GDPR erasure request), including rewriting any files that still contain the deleted records.",
        chipsLabel: 'Policy Actions', chips: ['Hot &rarr; Cool &rarr; Archive tiering', 'Snapshot expiration', 'Retention-limit deletion', 'GDPR erasure requests'],
        artifactTitle: 'Lifecycle Policy',
        artifactCode: `{
  "table": "raw.events",
  "hot_days": 30,
  "cool_days": 180,
  "archive_after_days": 365,
  "delete_after_days": 2555
}`,
        integration: [
          'Azure Blob Storage lifecycle policies — automated tiering',
          'Apache Iceberg snapshot expiration — bounds time-travel storage overhead',
          'Governance &amp; Security — defines retention and erasure requirements',
          'Scheduled .NET Core job (Azure Functions Timer) — executes the policy',
        ],
        servicesConsumed: [
          'Owning microservice &mdash; <code>Cxos.Foundation.Infrastructure</code> (see the <a href="../../index.html#service-map">Full Application Service Map</a>)',
          'Database &mdash; Azure Data Lake Storage Gen2 (Iceberg) &mdash; the lakehouse itself',
        ],
        nfr: [
          'Scale: tiering and expiration run as background jobs sized to process the full lakehouse on a rolling schedule without impacting query performance',
          'Latency: not applicable — this is a background cost/compliance process, not a query-path concern',
          "Reliability: deletion requests are tracked and confirmed, since 'right to be forgotten' compliance requires proof of completion",
          'Security/Privacy: this is itself a core privacy control — retention limits and erasure requests are only meaningful if actually enforced',
        ],
        example: 'A customer submits a data deletion request under GDPR. The lifecycle management job locates every table containing their records, rewrites the affected files to exclude them, and the compliance team receives a confirmation report — turning a manual, error-prone process into an automated, auditable one.',
      },
    ],
  },
  {
    anchor: 'metadata-layer', name: 'Metadata Layer',
    items: [
      {
        slug: 'catalog', name: 'Catalog',
        tagline: 'The searchable index of every table, its location, schema, and owner across the entire lakehouse — the platform\'s map of itself.',
        hldCaption: 'The Catalog is how anyone finds out what data already exists.',
        hld: [
          { label: 'Data Source', name: 'Data Sources', detail: 'Every touchpoint and business system' },
          { label: 'Ingestion', name: 'Ingestion Layer', detail: 'SDKs, connectors, protocols' },
          { label: 'Processing', name: 'Data Lakehouse', detail: 'Every table is registered here automatically' },
          { label: 'Foundation', name: 'Catalog', detail: 'Azure Purview — searchable table/schema index', origin: true },
          { label: 'Intelligence', name: 'Query &amp; Analytics Engine', detail: 'Resolves table locations via the catalog' },
          { label: 'Activation', name: 'Developer Portal', detail: 'Self-service table discovery' },
        ],
        business: [
          'Without a catalog, "does this data already exist somewhere" becomes tribal knowledge — the catalog makes discovery self-service',
          "Reduces duplicate data pipelines built because a team didn't know an existing table already had what they needed",
          'Owned by Data Governance / Platform Engineering',
        ],
        technical: 'The catalog is implemented on Azure Purview, which automatically scans and registers every table in the Data Lakehouse — location, schema, partition spec, and owner metadata. The Query &amp; Analytics Engine resolves logical table names to physical Iceberg table locations through the catalog rather than hardcoded paths, and the developer portal exposes catalog search so any engineer can discover existing tables before building a new pipeline.',
        chipsLabel: 'Catalog Contents', chips: ['Table location & schema', 'Owner metadata', 'Partition spec', 'Last-updated timestamp'],
        artifactTitle: 'Catalog Entry',
        artifactCode: `{
  "table": "curated.identity_stitched_events",
  "owner_team": "platform-engineering",
  "location": "abfss://lakehouse@cxosdata.dfs.core.windows.net/curated/events",
  "row_count_estimate": 2847193021,
  "last_updated": "2026-08-01T09:00:00Z"
}`,
        integration: [
          'Azure Purview — the catalog implementation',
          'Query &amp; Analytics Engine — resolves table names via catalog lookups',
          'Developer portal — surfaces catalog search to engineers',
          'Data Lakehouse — every table is auto-registered on creation',
        ],
        servicesConsumed: [
          'Owning microservice &mdash; <code>Cxos.Foundation.Api</code> (see the <a href="../../index.html#service-map">Full Application Service Map</a>)',
          'Database &mdash; ADLS Gen2 (Iceberg) + Azure Database for PostgreSQL (policy/retention state)',
        ],
        nfr: [
          'Scale: catalog scanning runs incrementally, not a full rescan of the lakehouse on every change',
          'Latency: catalog lookups are cached and fast; not a bottleneck for query planning',
          'Reliability: catalog staleness is bounded by the scan schedule — typically near-real-time for new tables',
          'Security/Privacy: catalog entries expose schema and metadata, not row-level data, so browsing the catalog is lower-risk than querying a table directly',
        ],
        example: "A new analyst wants revenue data and searches the catalog for 'revenue' instead of asking around — finding the existing marts.revenue_by_category_daily table and its owning team in seconds, avoiding a duplicate pipeline.",
      },
      {
        slug: 'lineage', name: 'Lineage',
        tagline: 'Traces exactly which upstream sources and transformations produced any given table or field — the platform\'s answer to "where did this number come from".',
        hldCaption: 'Lineage turns "where did this come from" into a queryable graph.',
        hld: [
          { label: 'Data Source', name: 'Data Sources', detail: 'Every touchpoint and business system' },
          { label: 'Ingestion', name: 'Ingestion Layer', detail: 'SDKs, connectors, protocols' },
          { label: 'Processing', name: 'Transformation &amp; Processing', detail: 'Every job reports its inputs/outputs' },
          { label: 'Foundation', name: 'Lineage', detail: 'Azure Purview — traces tables back to their sources', origin: true },
          { label: 'Intelligence', name: 'Impact Analysis', detail: 'What breaks if this table changes' },
          { label: 'Activation', name: 'Root-Cause Debugging', detail: 'Where a bad number is traced back to its origin' },
        ],
        business: [
          'When a number looks wrong, lineage is what lets someone trace it back to its source instead of guessing',
          'Enables impact analysis before a schema or pipeline change: "what downstream tables/dashboards does this affect"',
          'Owned by Data Governance / Platform Engineering',
        ],
        technical: "Every batch and stream job registers its inputs and outputs with Azure Purview's lineage API as part of its execution, building a directed graph from raw source tables through every transformation to final marts and dashboards. This is automatic instrumentation built into the shared batch/stream job frameworks, not something each team has to remember to add manually.",
        chipsLabel: 'Lineage Graph Nodes', chips: ['Source tables', 'Transformation jobs', 'Output tables', 'Downstream dashboards'],
        artifactTitle: 'Lineage Query Result',
        artifactCode: `marts.revenue_by_category_daily
  <- job: rollups_aggregations (daily)
  <- curated.order_fact
    <- job: data_modeling (hourly)
    <- curated.identity_stitched_events
      <- job: stream_worker (real-time)
      <- raw.events`,
        integration: [
          'Azure Purview — lineage graph storage and API',
          'Batch/Stream job frameworks — auto-instrumented to report lineage on every run',
          'Catalog — lineage is displayed alongside catalog entries',
          'Impact-analysis tooling — used before schema changes or deprecations',
        ],
        servicesConsumed: [
          'Owning microservice &mdash; <code>Cxos.Foundation.Api</code> (see the <a href="../../index.html#service-map">Full Application Service Map</a>)',
          'Database &mdash; ADLS Gen2 (Iceberg) + Azure Database for PostgreSQL (policy/retention state)',
        ],
        nfr: [
          'Scale: lineage graph grows with the number of tables and jobs, not event volume — manageable at any realistic pipeline count',
          'Latency: lineage updates as jobs run; current to the pipeline execution level, not the event level',
          "Reliability: auto-instrumentation means lineage doesn't silently go stale because a team forgot to document it",
          'Security/Privacy: lineage metadata is generally lower-sensitivity than the data itself, but access is still governed',
        ],
        example: 'A dashboard shows an unexpected revenue drop. Instead of guessing, the analyst traces the metric\'s lineage back through the marts, curated, and raw tables and finds a specific upstream connector had a data-quality issue that day — cutting root-cause time from hours to minutes.',
      },
      {
        slug: 'data-dictionary', name: 'Data Dictionary',
        tagline: 'Human-readable definitions for every table and field — what it means, not just what type it is.',
        hldCaption: 'The Data Dictionary adds meaning on top of the technical schema.',
        hld: [
          { label: 'Data Source', name: 'Data Sources', detail: 'Every touchpoint and business system' },
          { label: 'Ingestion', name: 'Ingestion Layer', detail: 'SDKs, connectors, protocols' },
          { label: 'Processing', name: 'Event Schema &amp; Registry', detail: 'Technical schema; dictionary adds business meaning' },
          { label: 'Foundation', name: 'Data Dictionary', detail: 'Business-readable field definitions in Azure Purview', origin: true },
          { label: 'Intelligence', name: "Semantic Layer", detail: "Query & Analytics Engine surfaces definitions to business users" },
          { label: 'Activation', name: 'Self-Service Analytics', detail: 'Business users understand fields without asking engineering' },
        ],
        business: [
          "A schema tells you a field's type; a dictionary tells you what it means and how to use it correctly — both are necessary for self-service analytics",
          'Reduces the "what does this column actually mean" questions that otherwise land on data engineering',
          'Owned by Analytics Engineering, with definitions contributed by each business domain',
        ],
        technical: "The data dictionary layers business-readable descriptions, valid value lists, and usage notes on top of the technical schema stored in the Catalog, managed through Azure Purview's business glossary feature. Fields can be linked to a canonical business term (e.g., 'Customer Lifetime Value') with a single definition shared across every table that includes that concept, rather than each table's schema comment drifting independently.",
        chipsLabel: 'Dictionary Contents', chips: ['Business-readable descriptions', 'Valid value lists', 'Canonical term linking', 'Usage notes/caveats'],
        artifactTitle: 'Dictionary Entry',
        artifactCode: `{
  "field": "marts.customer_ltv.ltv_band",
  "business_term": "Customer Lifetime Value Band",
  "description": "Tier bucket derived from trailing-12-month LTV",
  "valid_values": ["bronze", "silver", "gold", "platinum"]
}`,
        integration: [
          'Azure Purview business glossary — the dictionary implementation',
          'Catalog — technical schema that the dictionary adds meaning on top of',
          'Query &amp; Analytics Engine\'s semantic layer — surfaces definitions in BI tools',
          'Domain teams — contribute and own definitions for their business terms',
        ],
        servicesConsumed: [
          'Owning microservice &mdash; <code>Cxos.Foundation.Api</code> (see the <a href="../../index.html#service-map">Full Application Service Map</a>)',
          'Database &mdash; ADLS Gen2 (Iceberg) + Azure Database for PostgreSQL (policy/retention state)',
        ],
        nfr: [
          'Scale: dictionary entries grow with business term count, not data volume — a curation effort, not an infrastructure concern',
          'Latency: not applicable — this is reference metadata, not a runtime dependency',
          'Reliability: canonical term linking prevents definition drift across tables that reuse the same concept',
          'Security/Privacy: dictionary entries can flag a field as PII-adjacent even before formal classification is applied',
        ],
        example: "A marketing analyst building a self-service dashboard hovers over 'ltv_band' and sees its business definition and valid values directly in the BI tool, instead of pinging data engineering to ask what the field means.",
      },
      {
        slug: 'policies-entitlements', name: 'Policies &amp; Entitlements',
        tagline: 'The metadata-driven layer that determines who can access which tables and fields — access control expressed as data, not hardcoded application logic.',
        hldCaption: 'Policy as data means it is enforced consistently everywhere.',
        hld: [
          { label: 'Data Source', name: 'Data Sources', detail: 'Every touchpoint and business system' },
          { label: 'Ingestion', name: 'Ingestion Layer', detail: 'SDKs, connectors, protocols' },
          { label: 'Processing', name: 'Governance &amp; Security', detail: 'Defines the policy rules enforced here' },
          { label: 'Foundation', name: 'Policies &amp; Entitlements', detail: 'Metadata-driven access rules in Azure Purview', origin: true },
          { label: 'Intelligence', name: 'Access Control (RBAC/ABAC)', detail: 'Enforces these policies at query time' },
          { label: 'Activation', name: 'Every Query &amp; Consumer', detail: 'Policy is enforced consistently regardless of access path' },
        ],
        business: [
          "Centralizing access policy as metadata means it's enforced consistently everywhere, instead of each tool implementing its own access checks",
          'Makes access audits tractable — policies are inspectable data, not scattered application code',
          'Owned by Data Governance / Security',
        ],
        technical: 'Policies are expressed as metadata attached to catalog entries in Azure Purview — which Azure AD groups or roles can read which tables/columns, often keyed off the classification tags set during schema governance. The Query &amp; Analytics Engine and any other lakehouse-reading service checks these entitlements at query time via a shared policy-evaluation library, rather than each service reimplementing its own access logic.',
        chipsLabel: 'Policy Types', chips: ['Table-level access', 'Column-level masking', 'Row-level filtering', 'Purpose-based entitlements'],
        artifactTitle: 'Entitlement Policy',
        artifactCode: `{
  "table": "curated.customer_profile",
  "column": "email",
  "policy": "mask",
  "allowed_roles": ["data-engineering", "customer-support-tier2"]
}`,
        integration: [
          'Azure Purview — policy/entitlement metadata storage',
          'Azure AD (Entra ID) — role and group source of truth',
          'Shared policy-evaluation library — used by every lakehouse-reading service',
          "Governance & Security's Access Control — the enforcement mechanism this metadata drives",
        ],
        servicesConsumed: [
          'Owning microservice &mdash; <code>Cxos.Foundation.Api</code> (see the <a href="../../index.html#service-map">Full Application Service Map</a>)',
          'Database &mdash; ADLS Gen2 (Iceberg) + Azure Database for PostgreSQL (policy/retention state)',
        ],
        nfr: [
          'Scale: policy evaluation is a metadata lookup, not a per-row computation, keeping overhead low even on large tables',
          'Latency: policy checks add negligible latency to query execution',
          'Reliability: centralized policy means a single update propagates everywhere immediately',
          'Security/Privacy: policy metadata itself is tightly access-controlled to prevent privilege escalation',
        ],
        example: 'A new customer-support tool needs read access to customer profiles, but email addresses should stay masked for tier-1 agents. The entitlement policy is defined once and enforced automatically for every service that queries the table, rather than the support tool needing its own bespoke masking logic.',
      },
    ],
  },
  {
    anchor: 'governance-security', name: 'Governance &amp; Security',
    items: [
      {
        slug: 'access-control', name: 'Access Control (RBAC/ABAC)',
        tagline: 'Enforces who can read, write, or modify lakehouse data based on role and attribute — the runtime enforcement of the Policies &amp; Entitlements metadata.',
        hldCaption: 'Access Control is checked before any data is returned, on every query.',
        hld: [
          { label: 'Data Source', name: 'Data Sources', detail: 'Every touchpoint and business system' },
          { label: 'Ingestion', name: 'Ingestion Layer', detail: 'SDKs, connectors, protocols' },
          { label: 'Processing', name: "Policies &amp; Entitlements", detail: "Metadata Layer's rules enforced here" },
          { label: 'Foundation', name: 'Access Control (RBAC/ABAC)', detail: 'Azure AD roles (ADFS-federated for internal staff) + ABAC', origin: true },
          { label: 'Intelligence', name: 'Every Query', detail: 'Checked before any data is returned' },
          { label: 'Activation', name: 'Compliant Data Access', detail: 'The outcome this control guarantees' },
        ],
        business: [
          'The direct control that prevents unauthorized access to customer data — a compliance and trust requirement, not optional',
          'Role-based access lets the platform scale to many teams without a manually managed permission list per table',
          'Owned by Security / Data Governance',
        ],
        technical: "RBAC assigns broad table-level permissions via Azure AD (Entra ID) group membership (e.g., 'data-engineering' can read raw and curated zones). Internal/employee identities — support agents, analysts, engineers — authenticate through the enterprise's existing on-prem Active Directory, federated into Azure AD via ADFS (SAML/WS-Federation), so staff use their corporate SSO credentials rather than a separate Azure-only account; customer-facing identities remain on Azure AD B2C, kept entirely separate. ABAC layers finer-grained, attribute-based rules on top of either identity source — e.g., 'customer-support agents can read profile data only for customers in their assigned region.' Both are evaluated by the same shared policy library described under Policies &amp; Entitlements, checked on every query before data is returned.",
        chipsLabel: 'Model', chips: ['RBAC — role-based (Azure AD groups)', 'ABAC — attribute-based (finer-grained rules)', 'ADFS — federated internal/employee SSO'],
        artifactTitle: 'Access Decision',
        artifactCode: `{
  "principal": "agt_302",
  "identity_source": "adfs-federated",
  "role": "customer-support-tier1",
  "resource": "curated.customer_profile",
  "decision": "allow",
  "constraints": { "column_mask": ["email", "phone"] }
}`,
        integration: [
          'Azure AD (Entra ID) — role and group source of truth',
          'ADFS — federates on-prem enterprise Active Directory into Azure AD for internal/employee SSO',
          'Policies &amp; Entitlements metadata — defines the rules RBAC/ABAC evaluates',
          'Shared policy-evaluation library — enforced consistently across every lakehouse-reading service',
          'Audit Logs — every access decision is logged',
        ],
        servicesConsumed: [
          'Owning microservice &mdash; <code>Cxos.Foundation.Api</code> (see the <a href="../../index.html#service-map">Full Application Service Map</a>)',
          'Database &mdash; ADLS Gen2 (Iceberg) + Azure Database for PostgreSQL (policy/retention state)',
        ],
        nfr: [
          'Scale: policy evaluation adds negligible overhead per query regardless of lakehouse size',
          'Latency: sub-millisecond policy checks via cached role/attribute lookups',
          'Reliability: fail-closed by design — an evaluation error denies access rather than defaulting to allow',
          'Security/Privacy: a foundational security control; access decisions are themselves logged for audit, and ADFS federation means no separate password store exists for internal accounts to compromise',
        ],
        example: "A support agent signs in via ADFS-federated SSO using their existing corporate AD credentials, based in the EU, and tries to query a US customer's profile. ABAC's region-attribute rule denies the request even though their RBAC role would otherwise permit customer-profile access — a more precise control than role alone could provide.",
      },
      {
        slug: 'privacy-consent', name: 'Privacy &amp; Consent',
        tagline: 'The lakehouse-level enforcement of consent state — ensuring stored data can only be used for purposes the customer actually agreed to.',
        hldCaption: 'Consent is enforced at ingestion, at storage, and again at query time.',
        hld: [
          { label: 'Data Source', name: 'Data Sources', detail: 'Every touchpoint and business system' },
          { label: 'Ingestion', name: 'Consent Enforcement', detail: 'First check, at the edge, before storage' },
          { label: 'Processing', name: 'Transformation &amp; Processing', detail: 'Consent basis travels with every event' },
          { label: 'Foundation', name: 'Privacy &amp; Consent', detail: 'Purpose-based access rules in the lakehouse', origin: true },
          { label: 'Intelligence', name: 'Query &amp; Analytics Engine', detail: 'Filters query results by purpose and consent' },
          { label: 'Activation', name: 'Compliant Downstream Use', detail: 'Marketing/analytics only touch consented data' },
        ],
        business: [
          "Consent enforcement at ingestion isn't sufficient alone — this ensures stored historical data also respects consent changes made after storage",
          'Directly reduces regulatory risk under GDPR, CCPA, and DPDP',
          'Owned jointly by Legal/Privacy and Data Governance',
        ],
        technical: "Every stored event retains its <code>consent_basis</code> field from ingestion. When consent is withdrawn after the fact, a scheduled job updates the stored consent state so that any future query respecting purpose-based access — enforced the same way as Policies &amp; Entitlements — correctly excludes that customer's data from marketing-purpose queries going forward, even though the historical event still exists for legitimate purposes like fraud analysis.",
        chipsLabel: 'Enforcement Points', chips: ['Ingestion (Consent Enforcement)', 'Storage (consent_basis retained)', 'Query-time (purpose filtering)'],
        artifactTitle: 'Consent State Update',
        artifactCode: `{
  "customer_key": "cust_004821",
  "consent_basis": { "marketing": false, "analytics": true },
  "updated_at": "2026-08-01T12:00:00Z",
  "reason": "preference_center_withdrawal"
}`,
        integration: [
          'Consent Enforcement (Ingestion Layer) — first enforcement point, at the edge',
          "Governance & Security's policy engine — purpose-based query-time filtering",
          'Preference center — source of consent state changes',
          'Query &amp; Analytics Engine — respects consent_basis on every purpose-tagged query',
        ],
        servicesConsumed: [
          'Owning microservice &mdash; <code>Cxos.Foundation.Api</code> (see the <a href="../../index.html#service-map">Full Application Service Map</a>)',
          'Database &mdash; ADLS Gen2 (Iceberg) + Azure Database for PostgreSQL (policy/retention state)',
        ],
        nfr: [
          'Scale: consent state is a small, frequently-read field per customer — well within normal query patterns',
          'Latency: consent updates from the preference center propagate to enforcement within minutes',
          'Reliability: consent state changes are versioned, so historical "what was consent at time X" questions remain answerable',
          "Security/Privacy: this is itself the core privacy control the rest of the platform's compliance posture depends on",
        ],
        example: 'A customer withdraws marketing consent. Even though their historical events remain in the lakehouse for legitimate analytics and fraud-prevention purposes, every marketing-purpose query from that point forward automatically excludes them — without any team needing to manually update a suppression list.',
      },
      {
        slug: 'data-classification', name: 'Data Classification',
        tagline: 'Tags every field with a sensitivity level (public, internal, PII, sensitive-PII) — the foundation every other privacy and security control builds on.',
        hldCaption: 'Nothing else in Governance & Security works without classification first.',
        hld: [
          { label: 'Data Source', name: 'Data Sources', detail: 'Every touchpoint and business system' },
          { label: 'Ingestion', name: "Governance Rules", detail: "Event Schema & Registry mandates classification at registration" },
          { label: 'Processing', name: 'Transformation &amp; Processing', detail: 'Classification tags propagate through derived fields' },
          { label: 'Foundation', name: 'Data Classification', detail: 'Sensitivity tags on every lakehouse field', origin: true },
          { label: 'Intelligence', name: 'Access Control (RBAC/ABAC)', detail: 'Uses classification to drive masking rules' },
          { label: 'Activation', name: 'Encryption &amp; Consent Enforcement', detail: 'Both keyed off classification tags' },
        ],
        business: [
          'Nothing else in Governance &amp; Security works without this — access control, encryption scope, and consent enforcement all key off classification',
          'Gives the business a defensible, consistent answer to "how do you know where your sensitive data is"',
          'Owned by Data Governance, enforced at Schema Registry registration time',
        ],
        technical: "Every field is classified at schema registration time (enforced by the Event Schema &amp; Registry's Governance Rules) using a standard taxonomy: public, internal, PII, and sensitive-PII (financial, health). Classification tags are stored in the Catalog alongside each field's technical schema and propagate automatically to derived fields computed from a classified source — a field derived from an email address inherits the PII tag rather than starting unclassified.",
        chipsLabel: 'Classification Levels', chips: ['Public', 'Internal', 'PII', 'Sensitive-PII (financial, health)'],
        artifactTitle: 'Classification Tag',
        artifactCode: `{
  "field": "curated.customer_profile.email",
  "classification": "pii",
  "subcategory": "contact_info",
  "classified_by": "schema-registry-governance-rules",
  "classified_at": "2026-01-15T00:00:00Z"
}`,
        integration: [
          "Event Schema & Registry's Governance Rules — enforces classification at registration time",
          'Catalog — stores classification alongside technical schema',
          'Access Control (RBAC/ABAC) — masking rules are driven by classification',
          'Encryption — sensitive-PII may warrant stronger encryption keys/rotation policy',
        ],
        servicesConsumed: [
          'Owning microservice &mdash; <code>Cxos.Foundation.Api</code> (see the <a href="../../index.html#service-map">Full Application Service Map</a>)',
          'Database &mdash; ADLS Gen2 (Iceberg) + Azure Database for PostgreSQL (policy/retention state)',
        ],
        nfr: [
          'Scale: classification is metadata, not a runtime computation — no performance concern',
          'Latency: not applicable — classification is a design-time and registration-time control',
          'Reliability: automatic propagation to derived fields prevents classification gaps as new fields are computed',
          'Security/Privacy: the foundational control — every other privacy/security mechanism in this handbook depends on classification being correct',
        ],
        example: "A new derived field, customer_email_domain, is computed from the classified email field. Because classification propagates automatically, it correctly inherits at least an 'internal' sensitivity tag rather than defaulting to unclassified and silently bypassing access controls built around classification.",
      },
      {
        slug: 'audit-logs', name: 'Audit Logs',
        tagline: 'The immutable record of every access, change, and policy decision across the platform — what makes every other control provable, not just claimed.',
        hldCaption: 'Audit Logs are the evidence layer behind every governance control.',
        hld: [
          { label: 'Data Source', name: 'Data Sources', detail: 'Every touchpoint and business system' },
          { label: 'Ingestion', name: 'Consent Enforcement', detail: 'Every consent decision is logged here' },
          { label: 'Processing', name: 'Access Control (RBAC/ABAC)', detail: 'Every access decision is logged here' },
          { label: 'Foundation', name: 'Audit Logs', detail: 'Immutable, append-only log store', origin: true },
          { label: 'Intelligence', name: 'Compliance Reporting', detail: 'The primary consumer of audit data' },
          { label: 'Activation', name: 'Regulatory &amp; Internal Investigations', detail: 'What audit logs ultimately support' },
        ],
        business: [
          'Compliance frameworks (SOC 2, GDPR, ISO 27001) require provable audit trails, not just policy documents claiming controls exist',
          'Turns "did anyone access this customer\'s data inappropriately" from an unanswerable question into a queryable log',
          'Owned by Security / Compliance',
        ],
        technical: 'Every governance-relevant action — consent decisions, access-control evaluations, schema governance rejections, lifecycle deletions — writes an immutable, append-only record to a dedicated audit log store (Azure Data Lake Storage Gen2, write-once configuration, separate from operational tables). Audit records are structured and queryable, letting Compliance run investigations without needing to grep application logs.',
        chipsLabel: 'Logged Events', chips: ['Consent decisions', 'Access-control evaluations', 'Schema governance rejections', 'Lifecycle deletions'],
        artifactTitle: 'Audit Log Entry',
        artifactCode: `{
  "timestamp": "2026-08-01T12:00:03Z",
  "actor": "agt_302",
  "action": "query",
  "resource": "curated.customer_profile",
  "decision": "allow",
  "policy_ref": "abac-region-rule-4"
}`,
        integration: [
          'Azure Data Lake Storage Gen2 (write-once configuration) — immutable audit log storage',
          'Consent Enforcement, Access Control, Governance Rules — all write audit entries',
          'Compliance reporting tools — query the audit log store',
          'Azure Monitor — operational alerting on suspicious audit patterns',
        ],
        servicesConsumed: [
          'Owning microservice &mdash; <code>Cxos.Foundation.Api</code> (see the <a href="../../index.html#service-map">Full Application Service Map</a>)',
          'Database &mdash; ADLS Gen2 (Iceberg) + Azure Database for PostgreSQL (policy/retention state)',
        ],
        nfr: [
          'Scale: audit logging is append-only and write-optimized, sized for governance-relevant decision volume, not raw event volume',
          'Latency: audit writes are asynchronous relative to the action they log, so they never slow down the primary operation',
          'Reliability: write-once storage configuration prevents tampering, including by administrators',
          'Security/Privacy: access to the audit log itself is tightly restricted — auditing the auditors is a standard security requirement',
        ],
        example: "A regulator asks for proof that a specific customer's marketing-consent withdrawal was honored. Compliance queries the audit log and produces a complete, tamper-proof record of the consent change and every subsequent access decision — turning a multi-week manual investigation into a same-day query.",
      },
      {
        slug: 'encryption', name: 'Encryption (At Rest &amp; In Transit)',
        tagline: "Ensures data is unreadable to anyone without proper authorization, whether it's sitting in storage or moving between services.",
        hldCaption: 'Encryption covers every byte, whether stored or moving.',
        hld: [
          { label: 'Data Source', name: 'Data Sources', detail: 'Every touchpoint and business system' },
          { label: 'Ingestion', name: 'TLS 1.2+', detail: 'Every ingestion transport is encrypted in transit' },
          { label: 'Processing', name: 'Azure Event Hubs', detail: 'Encrypted in transit between services' },
          { label: 'Foundation', name: 'Encryption (At Rest &amp; In Transit)', detail: 'Azure Storage Service Encryption + Azure Key Vault', origin: true },
          { label: 'Intelligence', name: 'Azure Key Vault', detail: 'Manages and rotates encryption keys' },
          { label: 'Activation', name: 'Every Stored &amp; Moving Byte', detail: 'Nothing in the platform is unencrypted' },
        ],
        business: [
          'A baseline, non-negotiable control for any platform handling customer data — required by virtually every compliance framework and customer contract',
          'Limits the blast radius of a storage-layer or network-layer breach even if other controls fail',
          'Owned by Security / Platform Engineering',
        ],
        technical: 'Data at rest in Azure Data Lake Storage Gen2 is encrypted using Azure Storage Service Encryption (SSE), with keys managed and rotated through Azure Key Vault — customer-managed keys are used for sensitive-PII-classified data. Data in transit is encrypted via TLS 1.2+ across every hop: SDK-to-API Management, API Management-to-Ingestion API, Ingestion API-to-Event Hubs, and every internal service-to-service call, with mTLS for internal traffic where the Zero-Trust network policy requires it.',
        chipsLabel: 'Coverage', chips: ['At rest: Azure Storage Service Encryption', 'In transit: TLS 1.2+ / mTLS', 'Key management: Azure Key Vault'],
        artifactTitle: 'Encryption Configuration',
        artifactCode: `{
  "storage_account": "cxosdata",
  "encryption": "customer-managed-key",
  "keyvault_ref": "cxos-prod-cmk",
  "min_tls_version": "1.2"
}`,
        integration: [
          'Azure Storage Service Encryption — at-rest encryption for the Data Lakehouse',
          'Azure Key Vault — key management and rotation',
          'TLS 1.2+ / mTLS — in-transit encryption across every service boundary',
          "Azure AD managed identities — used instead of long-lived keys where possible to reduce exposure further",
        ],
        servicesConsumed: [
          'Owning microservice &mdash; <code>Cxos.Foundation.Api</code> (see the <a href="../../index.html#service-map">Full Application Service Map</a>)',
          'Database &mdash; ADLS Gen2 (Iceberg) + Azure Database for PostgreSQL (policy/retention state)',
        ],
        nfr: [
          'Scale: encryption/decryption overhead is negligible relative to network and query time at this scale',
          "Latency: TLS handshake overhead is amortized via connection reuse across the platform's service mesh",
          'Reliability: key rotation is automated and does not require downtime or a data re-encryption pass for most rotation types',
          'Security/Privacy: a baseline, table-stakes control that complements classification, access control, and consent enforcement rather than replacing them',
        ],
        example: "A misconfigured network ACL briefly exposes a storage endpoint. Because data at rest is encrypted with customer-managed keys the exposed endpoint's identity doesn't have access to, the exposure doesn't translate into a readable data breach — encryption limits the damage from a control failure elsewhere.",
      },
    ],
  },
];
